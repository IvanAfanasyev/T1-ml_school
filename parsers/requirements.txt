# ── HTTP / парсинг HTML ─────────────────────────────────────────────────────
requests>=2.31
beautifulsoup4>=4.12

# ── Браузерный рендеринг (используется в vk_cloud_parser) ───────────────────
# После установки нужно один раз выполнить: playwright install chromium
playwright>=1.40

# ── PDF / таблицы (t1-cloud parsing) ────────────────────────────────────────
pdfplumber>=0.10
pandas>=2.0
openpyxl>=3.1

# ── LLM ─────────────────────────────────────────────────────────────────────
openai>=1.30

# ── Конфигурация / схемы ────────────────────────────────────────────────────
pydantic>=2.5
pydantic-settings>=2.1
